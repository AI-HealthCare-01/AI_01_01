# AGENTS.md

## 목적
이 저장소는 MindSight 웹서비스 구현용 레포다.  
이 레포의 목표는 `blueprint/` 아래의 기능 설계를 실제 코드로 안전하게 구현하는 것이다.

우선순위:
1. 기존 blueprint를 기준으로 구현
2. 민감정보/권한/안전 규칙 준수
3. 로컬 개발 가능 상태 유지
4. 기능 단위로 작게 구현하고 검증
5. 문서와 코드의 불일치 최소화

---

## 소스 오브 트루스
기능 구현의 1차 기준은 `blueprint/` 폴더다.

저장소 전역 규칙:
- 기능 구현의 source of truth는 `blueprint/`다.
- 기존 구현 코드는 참고 자료이며, `blueprint/`와 충돌하면 `blueprint/`를 우선한다.
- `blueprint`와 구현이 충돌할 때 임의 판단으로 덮어쓰지 말고, 차이를 문서화한 뒤 반영한다.

각 기능은 아래 blueprint를 먼저 읽고 구현한다.
- `blueprint/auth_account/`
- `blueprint/checkin/`
- `blueprint/assessment_scales/`
- `blueprint/challenge/`
- `blueprint/journal/`
- `blueprint/activity_log/`
- `blueprint/dashboard/`
- `blueprint/report_summary/`
- `blueprint/board_feed/`
- `blueprint/support_feedback/`
- `blueprint/mypage/`
- `blueprint/admin_console/`
- 기타 추가 blueprint

기능 구현 전에:
1. 루트 `AGENTS.md`
2. 해당 기능의 `blueprint/<feature>/AGENTS.md`
3. 해당 기능의 `blueprint/<feature>/README.md`
순서로 읽는다.

---

## 작업 원칙
- `blueprint/` 문서는 삭제하지 않는다.
- 기능 설계와 다르게 구현해야 할 이유가 있으면, 코드에 바로 덮어쓰지 말고 변경 근거를 문서에 남긴다.
- 모르는 것이 있으면 임의로 크게 바꾸지 말고, 현재 설계를 최대한 보존한다.
- 한 번에 전체 서비스를 다 구현하지 말고, 기능 단위로 나눠서 구현한다.
- 큰 리팩토링보다 작은 단계별 변경을 선호한다.
- 기존 동작을 깨지 않도록 한다.
- 더미/임시 코드는 최소화한다.
- 민감정보는 최소 수집/최소 노출 원칙을 따른다.
- production 사용자 화면에는 `preview/demo/showcase/developer note/helper` 성격의 문구를 남기지 않는다.
- 핵심 사용자 기능은 상단 메뉴 또는 하위 메뉴를 통해 접근 가능해야 한다.
- 현 단계에서는 디자인 전면 수정/스타일 대수술보다 기능 완결성과 정보구조 정합을 우선한다.
- wireframe 부재 상태에서는 시각 실험보다 메뉴 구조, 라우팅, 기능 진입 흐름을 먼저 맞춘다.

---

## 현재 전제
이 저장소는 다음 전제를 가진다.
- 웹 프론트엔드: Next.js + TypeScript
- 백엔드/API: Python 기반 API
- DB: PostgreSQL
- 인증: Firebase Authentication
- 파일 저장: S3 또는 호환 스토리지
- 배포: AWS
- 로컬 개발 후 GitHub로 협업

실제 구현 중 일부 스택이 달라지더라도, 이 문서의 구조/보안/권한 원칙은 유지한다.

---

## 디렉터리 원칙
루트 기준으로 다음 역할을 가진다.

- `blueprint/` : 설계 문서 원본
- `model/` : 데이터셋, 모델, 메트릭, 스키마
- `design/` : 레퍼런스 이미지, 스타일 브리프, UI 규칙
- `apps/web/` : 프론트엔드 앱
- `apps/api/` : 백엔드/API 앱
- `packages/shared/` : 공통 유틸, 타입, 상수
- `packages/domain/` : 도메인 모델, 스키마, 계약
- `packages/cbt-engine/` : CBT 관련 구조화 로직
- `infra/aws/` : AWS 인프라 코드/문서
- `docs/` : 개발 문서, 워크플로우, 저장소 맵
- `scripts/` : 로컬 실행, 시드, 배치, 도구성 스크립트

폴더가 없으면 생성할 수 있다.  
단, 새 폴더를 만들 때는 기존 역할과 충돌하지 않게 한다.

---

## 구현 순서 원칙
기능 구현은 아래 순서를 기본으로 따른다.

1. 루트 스캐폴드 / 공통 규칙
2. 디자인 시스템
3. 인증 / 계정 / 온보딩
4. 체크인 / 설문 / 챌린지 / 일기 / 활동로그
5. CBT / 대시보드 / 요약리포트
6. 게시판 / 문의 / 마이페이지
7. 관리자 콘솔
8. 모델 연결 / 배포

---

## 인증/계정 원칙
- 이메일 인증 및 비밀번호 재설정은 Firebase Authentication 기준으로 구현한다.
- 이메일 인증은 "이메일 소유 확인" 용도로 정의한다.
- 회원가입 단계에서는 최소 정보만 수집한다.
  - 닉네임
  - 이메일
  - 비밀번호
  - 비밀번호 확인
  - 필수 동의
- 출생년도, 성별, 민감정보 동의, 초기 진단척도는 첫 로그인 후 온보딩에서 수집한다.
- 이메일 미인증 계정은 주요 기능 접근을 제한한다.
- 내부 ID는 분리한다.
  - `user_id`
  - `firebase_uid`
  - `ml_subject_id`
- `ml_subject_id`는 synthetic 데이터와 눈으로 구분 가능한 real-user 규칙을 유지한다.

---

## 개인정보 / 민감정보 / 안전 원칙
- 정신건강 관련 데이터는 민감정보로 취급한다.
- 민감정보는 기능 구현상 필요한 범위에서만 조회/저장/표시한다.
- 위험 플래그는 구조화된 신호만 사용한다.
- CBT/일기/문의 등의 자유 텍스트는 기본 화면에 과도하게 노출하지 않는다.
- 로그에는 원문 전체를 남기지 말고, 필요 최소한의 운영 로그만 남긴다.
- 관리자 콘솔은 권한별 최소 노출 원칙을 따른다.
- IP는 일반 사용자 조회 화면에 기본 노출하지 않는다.
- 차단/보안 조치 화면에서만 제한적으로 노출한다.

---

## 프론트엔드 원칙
- 모바일 우선으로 구현한다.
- 공통 컴포넌트와 디자인 토큰을 먼저 사용한다.
- 화면마다 loading / empty / error 상태를 만든다.
- 과도한 애니메이션, 과도한 게임화, 공격적인 색상 사용을 피한다.
- 정신건강 서비스 특성상 차분하고 예측 가능한 UI를 유지한다.
- 일관된 spacing, typography, card rule을 따른다.
- 새로운 UI 패턴을 만들기 전에 `design/style_brief.md` 와 공통 컴포넌트를 먼저 확인한다.

---

## 백엔드/API 원칙
- 라우트는 도메인별로 분리한다.
- 요청/응답 스키마를 명확히 유지한다.
- 에러 응답 형식을 통일한다.
- DB 스키마 변경은 마이그레이션 또는 명시적 스키마 문서와 같이 간다.
- 사용자용 요약 뷰와 모델링용 원천 로그를 구분한다.
- 활동로그는 원천 이벤트를 그대로 보여주지 말고 day 기준 요약으로 만든다.

---

## 모델 / 데이터 원칙
- 모델 입력용 테이블과 사용자 화면용 테이블을 혼동하지 않는다.
- synthetic 데이터와 real user 데이터는 ID 규칙으로 구분 가능해야 한다.
- 설문 점수는 sparse anchor로 취급한다.
- 현재 상태(nowcast)와 추세(7일/28일 retrospective)를 구분한다.
- 재학습/배포는 승인형 구조를 전제로 한다.
- 모델 변경은 관리자 콘솔의 승인 플로우와 연결될 수 있도록 구현한다.

---

## 게시판 / 문의 / 관리자 원칙
- 게시판은 최신순 피드 + 더보기 방식으로 구현한다.
- 신고 큐 / 유해언어 큐 / 안전 큐를 분리한다.
- 문의/피드백은 비공개 티켓 시스템으로 구현한다.
- 사용자 추가문의는 새 티켓이 아니라 기존 티켓 재오픈으로 처리한다.
- 관리자 권한 구조는 다음을 따른다.
  - Owner
  - Admin
  - Support
  - optional analyst/ml extension
- 정책 수정 및 모델 배포는 Owner 승인 후 반영한다.

---

## 테스트 / 검증 원칙
각 작업 단위마다 최소한 아래를 수행한다.
- build
- lint
- type check
- relevant test
- 실행 방법 README 반영

새 기능을 추가하면:
- 주요 happy path
- 주요 error path
- 권한/비인증 접근 제한
- 민감정보 노출 여부
를 확인한다.

---

## 문서화 원칙
변경이 있으면 아래 중 최소 하나는 같이 업데이트한다.
- 해당 기능 blueprint 관련 README/notes
- `docs/`
- 루트 README
- 환경변수 문서

코드와 문서가 다르면, 코드만 먼저 바꾸지 말고 문서도 같이 갱신한다.
`blueprint`와 구현 차이가 발생하면 `docs/acceptance_criteria.md` 기준에 따라 차이/근거/영향을 문서화한다.

---

## 금지 사항
- blueprint 무시하고 임의 구조로 전면 재설계
- 민감정보를 테스트 로그/콘솔에 그대로 출력
- 이메일 미인증 계정에 커뮤니티/문의/핵심 기능 전면 허용
- 루트에 임시 스크립트/임시 메인 파일 남기기
- 검증 없이 모델/정책 자동 배포
- 사용자 화면에 관리자용 내부 메모/판단 기준 노출

---

## 산출물 기대 형식
작업을 마치면 다음을 남긴다.
1. 변경된 파일 목록
2. 실행/검증 방법
3. 남은 TODO 또는 리스크
4. blueprint와 달라진 점(있다면)
