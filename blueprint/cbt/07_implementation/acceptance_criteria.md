# Acceptance criteria

- cbt_session_summary와 cbt_risk_signal이 분리 저장된다
- current-state model에 쓰는 export field와 internal hypothesis field가 구분된다
- risk_level >= 2 에서 safety-first 흐름이 동작한다
- challenge / free journal과 entry point가 분리된다
- wellness copy를 유지하고 진단/치료 단정 문구가 없다
