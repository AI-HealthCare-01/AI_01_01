# Codex 작업 규칙 (일기)

- 이 폴더는 `blueprint/journal/` 기준 설계다.
- 사용자 상위 메뉴명은 `일기`로 구현한다.
- 제목은 선택, 본문은 필수로 설계한다.
- 목록 화면에서는 제목 또는 첫 줄 일부만 보여주고, 전체 본문은 상세 화면에서만 보여준다.
- 저장/수정/삭제가 발생하면 활동로그 연동 ETL을 호출하거나 비동기 재집계를 트리거한다.
- 저널 텍스트는 v1 모델 입력에 직접 넣지 않는다. 기본 연동은 `작성 여부`, `작성 수`, `최근 작성일` 수준으로 제한한다.
